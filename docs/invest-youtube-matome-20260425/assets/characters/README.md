# assets/characters

diagram-invest 由来の `hiroko-*.png` / `ichisan-*.png` を置けば、`index.html` の `src` を `.png` に差し替えるだけで差し替え可能。  
正本: リポジトリの `.cursor/skills/invest-youtube-matome/references/character-avatars-compact.md`
